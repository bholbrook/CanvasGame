/* Notes

TODO:
Add score overlay
Add points for destroying enemies
Add timer with random enemies spawning in random top area of screen
Add specific box areas, each with an id, get an id randomly then draw an enemy inside the box. this area is the enemies entire life cycle box
Build better AI

NOTES:
Need to build a List object so that can replace all Arrays with Lists to use the removeAt(i) function to fix bug 1.
EX.
var r = Math.random() * 255>>0;
var g = Math.random() * 255>>0;
var b = Math.random() * 255>>0;


BUGS:
Running into an enemy removes all bullets associated with it too

*/